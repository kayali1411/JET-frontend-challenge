import { Layout } from '../../components/Layout';

const LoginScreen = () => {
  return (
    <Layout>
      <div>
        <h1>Login Screen</h1>
      </div>
    </Layout>
  );
};

export default LoginScreen;
